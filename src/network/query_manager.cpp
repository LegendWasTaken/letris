#include "query_manager.h"

void let::network::query::query_servers(const std::vector<queryable_server> &servers) {

}

std::vector<let::network::query::queryable_server_info> let::network::query::query_status() const noexcept {
    return std::vector<queryable_server_info>();
}
